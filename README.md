# Setup dotfiles
```bash
git clone https://github.com/ArthurDelannoyazerty/dotfiles.git ~/.dotfiles
sh ~/.dotfiles/setup.sh
```

# Modify dotfiles
I you  want to add a file somewhere, do not forget to add the map *local filepath* <--> *home filepath* in the `setup.sh` script